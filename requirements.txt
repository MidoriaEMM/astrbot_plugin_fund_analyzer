# AstrBot 基金分析插件依赖
akshare>=1.10.0
#pandas>=1.5.0
aiohttp>=3.8.0
matplotlib>=3.7.0
jinja2>=3.0.0
playwright>=1.40.0
markdown>=3.4.0
#新增tickflow 不行需要手动 pip install "tickflow[all]" --upgrade
#新增tushare pip install tushare
#删除numpy conapy（好像叫这个） 更新playwright 不需要pandas potable版本
