{
  "tickflow_api_key": {
    "description": "Tickflow API Key（股票实时行情，完整服务）。留空则从环境变量 TICKFLOW_API_KEY 读取",
    "type": "text",
    "default": "",
    "hint": "在 tickflow.org 控制台生成；此处非空时优先于环境变量 TICKFLOW_API_KEY"
  }
}
